spring:
  cloud:
    stream:
      bindings:
        input:
          destination: rooms
  rabbit:
    host: rabbitmq

eureka:
  client:
    serviceUrl:
      defaultZone: http://registry-service:8080/eureka
